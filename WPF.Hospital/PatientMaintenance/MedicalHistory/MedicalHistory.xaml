<Window x:Class="WPF.Hospital.MedicalHistory"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPF.Hospital"
        mc:Ignorable="d"
        Title="MedicalHistory" Height="450" Width="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,10">
            <TextBlock Text="Prescription History" FontSize="20" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,10"/>
            <TextBlock Text="Patient Information" FontWeight="SemiBold" Margin="0,10,0,5"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                <TextBlock Text="Patient ID: " VerticalAlignment="Center" Width="172"/>
                <TextBlock x:Name="txtPatientId" VerticalAlignment="Center" Width="121"/>
                <TextBlock Text="Name: " VerticalAlignment="Center" Margin="20,0,0,0" Width="76"/>
                <TextBlock x:Name="txtPatientName" VerticalAlignment="Center" Width="109"/>
            </StackPanel>
        </StackPanel>

        <DataGrid x:Name="dgMedicalHistory" Grid.Row="1" AutoGenerateColumns="False" 
              CanUserAddRows="False" CanUserDeleteRows="False" Margin="0,0,0,10" IsReadOnly="True"
              ItemsSource="{Binding HistoryList}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Doctor Name" Binding="{Binding DoctorFullName}" Width="*"/>
                <DataGridTextColumn Header="Prescription" Binding="{Binding Prescription}" Width="*"/>
                <DataGridTextColumn Header="Date" Binding="{Binding Date, StringFormat=d}" Width="150"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Add Prescription Section -->
        <GroupBox Header="Add New Prescription" Grid.Row="2" Margin="0,10,0,0">
            <StackPanel Orientation="Vertical" Margin="10">
                <WrapPanel Margin="0,0,0,5">
                    <TextBlock Text="Doctor Name:" VerticalAlignment="Center" Width="100"/>
                    <TextBox x:Name="txtDoctorName" Width="250" />
                    <TextBlock Text="Prescription:" VerticalAlignment="Center" Width="100" Margin="20,0,0,0"/>
                    <TextBox x:Name="txtPrescription" Width="250" />
                </WrapPanel>

                <WrapPanel Margin="0,10,0,0">
                    <Button x:Name="btnAddPrescriptiop" Content="Add Prescription" Height="30" Width="120" Margin="5" Click="btnAddPrescription_Click"/>
                    <Button x:Name="btnDeletePrescription" Content="Delete Prescription" Height="30" Width="130" Margin="5" Click="btnDeletePrescription_Click"/>
                    <Button x:Name="btnClearFields" Content="Clear Fields" Height="30" Width="100" Margin="5" Click="btnClearFields_Click"/>
                </WrapPanel>
            </StackPanel>
        </GroupBox>
    </Grid>
</Window>
