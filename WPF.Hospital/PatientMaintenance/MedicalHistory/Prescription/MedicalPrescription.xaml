<Window x:Class="WPF.Hospital.PatientMaintenance.MedicalHistory.Prescription.MedicalPrescription"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPF.Hospital.PatientMaintenance.MedicalHistory.Prescription"
        mc:Ignorable="d"
        Title="MedicalPrescription" Height="450" Width="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,10">
            <TextBlock Text="Prescription History" FontSize="20" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,10"/>
        </StackPanel>

        <DataGrid x:Name="dgMedicalHistory" Grid.Row="1" AutoGenerateColumns="False" 
              CanUserAddRows="False" CanUserDeleteRows="False" Margin="0,0,0,10" IsReadOnly="True"
              ItemsSource="{Binding PrescriptionList}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Medicine" Binding="{Binding Medicine.Name}" Width="*"/>
                <DataGridTextColumn Header="Quantity" Binding="{Binding Quantity}" Width="100"/>
                <DataGridTextColumn Header="Frequency" Binding="{Binding Frequency}" Width="150"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Add Prescription Section -->
        <GroupBox Header="Add New Prescription" Grid.Row="2" Margin="0,10,0,0">
            <StackPanel Orientation="Vertical" Margin="10">
                <WrapPanel Margin="0,0,0,5">
                    <TextBlock Text="Medicine:" VerticalAlignment="Center" Width="100"/>
                    <ComboBox x:Name="cbMedicine" Width="200" />
                    <TextBlock Text="Quantity:" VerticalAlignment="Center" Width="80" Margin="20,0,0,0"/>
                    <TextBox x:Name="txtQuantity" Width="100" />
                </WrapPanel>

                <WrapPanel Margin="0,0,0,5">
                    <TextBlock Text="Frequency:" VerticalAlignment="Center" Width="100"/>
                    <TextBox x:Name="txtFrequency" Width="300" />
                </WrapPanel>

                <WrapPanel Margin="0,10,0,0">
                    <Button x:Name="btnAddPrescriptiop" Content="Add Prescription" Height="30" Width="120" Margin="5" Click="btnAddPrescriptiop_Click"/>
                    <Button x:Name="btnDeletePrescription" Content="Delete Prescription" Height="30" Width="130" Margin="5" Click="btnDeletePrescription_Click"/>
                    <Button x:Name="btnClearFields" Content="Clear Fields" Height="30" Width="100" Margin="5" Click="btnClearFields_Click"/>
                </WrapPanel>
            </StackPanel>
        </GroupBox>
    </Grid>
</Window>
