<Window x:Class="WPF.Hospital.DeleteMedicine"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPF.Hospital"
        mc:Ignorable="d"
        Title="DeleteMedicine" Height="350" Width="600" WindowStartupLocation="CenterScreen">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Input -->
        <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0,0,0,10">
            <TextBlock Text="Medicine ID:" VerticalAlignment="Center" Margin="0,0,5,0"/>
            <TextBox x:Name="txtMedicineId" Width="100"/>
            <Button Content="Delete" Width="100" Margin="10,0,0,0" Click="btnDelete_Click"/>
        </StackPanel>

        <!-- Result -->
        <TextBlock x:Name="lblResult" Grid.Row="1" FontWeight="Bold" Margin="0,0,0,10"/>

        <!-- Instructions -->
        <TextBlock Grid.Row="2" Text="Enter the Medicine ID you want to delete and press Delete." 
                   Foreground="Gray" FontStyle="Italic"/>
    </Grid>
</Window>
